# Example client

A collection of example client application interacting with a provisioned cluster that check the cluster attestation before connecting.
In this repository are present three example clients: 
- [nginx_client](./nginx_client/) - Demonstrates connecting to an attested cluster provisioned with the [nginx package](../../operator/packages/zarf_nginx/).