[project]
name = "gcp-vm-cli"
version = "0.1.0"
description = "GCP VM CLI - Provision GCP Shielded VMs with attestation support"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "pydantic>=2.0.0",
]

[project.scripts]
gcp-vm-cli = "main:main"
