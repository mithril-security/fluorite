# Cloud Helper
Utility for automating some tasks on Cloud Platforms. Right now only Azure is supported.

# Azure Helper
Can create resource groups, upload disks (needs azcopy and qemu-img), create disks snapshots, create azure compute galleries, vm image definitions, versions for VM image definitions, and spawn vms based on an image 